# התקנה — הפנקס היומי על מחשב חדש

המערכת **מקומית לחלוטין** — אין ענן, שום מידע לא נשלח החוצה. כדי להעביר אותה למחשב אחר יש שתי דרכים.

---

## דרך א' — Docker (הכי פשוט, כל מערכת הפעלה)

דרוש **Docker Desktop** בלבד. מעתיקים את התיקייה `magnet-studio/` למחשב החדש ואז:

```bash
cd magnet-studio
cp .env.example .env        # אופציונלי — לערוך ולהוסיף מפתחות
docker compose up -d
```

פותחים: **http://localhost:4420/daily.html**

- כולל את מסד הדיור (Postgres) עם 40 נכסי דמו — עולה אוטומטית.
- ל-JARVIS מקומי דרך n8n:  `docker compose --profile n8n up -d`
- עצירה: `docker compose down` · עדכון גרסה: `docker compose up -d --build`
- המצב (הגדרות, גיבויים) נשמר ב-volume בשם `magnet-studio_ms_data`.

---

## דרך ב' — Node ישירות (בלי Docker)

דרוש **Node.js 20+**.

### Windows
```powershell
cd magnet-studio
.\install\install.ps1        # תלויות + Firewall + הפעלה אוטומטית בכניסה למחשב
npm start
```

### macOS / Linux
```bash
cd magnet-studio
bash install/install.sh      # תלויות + הפעלה אוטומטית (LaunchAgent / systemd)
npm start
```

פותחים: **http://localhost:4420/daily.html**

---

## העברת ההגדרות האישיות בין מחשבים (סנכרון ידני)

אין ענן, אז הסנכרון הוא קובץ שמעבירים:

1. במחשב הישן: **הגדרות** (בתפריט) → **ייצוא גיבוי** → נשמר `magnet-studio-backup-YYYY-MM-DD.json`.
2. מעבירים את הקובץ (מייל לעצמך / דיסק / Drive).
3. במחשב החדש: **הגדרות** → **ייבוא גיבוי** → בוחרים את הקובץ.
4. מפעילים מחדש את השרת.

**נכנס לגיבוי:** תאריך/שעת/עיר לידה (אסטרולוגיה), הגדרות JARVIS, נתיב תיקיית העיצובים, רשימת מוזיקת הרקע.
**לא נכנס:** מפתחות API (רק ב-`.env`/`setx` שלך), מטמוני היום (נבנים מחדש), מסד הדיור.

---

## התקנה כאפליקציה בטלפון (PWA — אנדרואיד / אייפון / כל מכשיר)

הפנקס הוא Progressive Web App — מתקינים אותו כאפליקציה מלאה **בלי חנות**.

1. הטלפון והמחשב על **אותה רשת Wi-Fi**.
2. במחשב: **הגדרות** באתר → סעיף "התקנה כאפליקציה" → יש שם **קוד QR** וכתובת (למשל `http://192.168.1.20:4420/daily.html`).
3. סורקים את ה-QR בטלפון (או מקלידים את הכתובת ב-Chrome).
4. תפריט הדפדפן **⋮** → **"הוסף למסך הבית"** / **"התקן אפליקציה"**.
5. נוצר אייקון. פתיחה ממנו = מסך מלא, בלי שורת כתובת, עם מטמון אופליין של המידע האחרון.

### אם הטלפון לא מצליח להתחבר
בדרך כלל: (1) הרשת מסומנת **Public** ו/או (2) פורט 4420 חסום בחומת האש. ב-Windows:

**דאבל-קליק על `install\פתח-גישה-לטלפון.bat`** ואשרו את חלון ה-UAC. הוא:
- מוסיף חוק Firewall לפורטים **4420** (הפנקס) ו-**3737** (מערכת הגרפולוגיה) — לכל הפרופילים
- הופך את רשת הבית מ-Public ל-Private
- מדפיס את הכתובת המדויקת לטלפון

אם עדיין לא: בהגדרות הראוטר, כבו **"AP Isolation" / "בידוד לקוחות"**.

### מה עובד ומה לא בטלפון
| | טלפון (http) | מחשב |
|---|---|---|
| מענה יומי, דיור, אסטרולוגיה, לוח בקרה | ✅ | ✅ |
| צ'אט JARVIS (הקלדה) | ✅ | ✅ |
| **דיבור בקול ל-JARVIS** | ❌ הדפדפן חוסם מיקרופון ללא HTTPS | ✅ |
| הקראה בקול של תשובות | ✅ (בדרך כלל) | ✅ |
| גרפולוגיה | ✅ (אחרי פתיחת פורט 3737) | ✅ |

**דיבור בקול בטלפון** דורש HTTPS. הדרך הפשוטה: [Tailscale](https://tailscale.com) — נותן כתובת HTTPS
אמיתית (`tailscale serve`) שעובדת גם מחוץ לבית. לא מובנה כרגע.

### מחוץ לבית (רשת אחרת)
צריך VPN אל המחשב הביתי — למשל [Tailscale](https://tailscale.com) (חינם לשימוש אישי): מתקינים על המחשב ועל
הטלפון, ואז הכתובת של המחשב ברשת Tailscale עובדת מכל מקום. לא מובנה במערכת.

---

## משתני סביבה

כל המפתחות אופציונליים. שתי דרכים:
- **קובץ `.env`** בתיקיית הפרויקט (מומלץ — עובד בכל מערכת). ראו `.env.example`.
- **`setx NAME "value"`** ב-Windows (משתנה סביבה קבוע), ואז להפעיל מחדש.

`.env` **לא** נכנס ל-git ולעולם לא מסונכרן בגיבוי. אל תשתפו אותו.
