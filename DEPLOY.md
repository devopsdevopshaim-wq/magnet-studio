# פריסה לכתובת ציבורית

המערכת בנויה לרוץ מקומית, אבל אפשר להעלות אותה לאינטרנט כדי לפתוח אותה מכל מקום —
כולל מהטלפון — בלי שהמחשב בבית יהיה דלוק.

---

## שלב 1 — הקוד ל‑GitHub (פעם אחת)

מתוך תיקיית `magnet-studio`:

```bash
git init
git add -A
git commit -m "magnet-studio"
```

צור repo **פרטי** ריק ב‑https://github.com/new (בלי README), ואז:

```bash
git remote add origin https://github.com/<שם-המשתמש>/<שם-הrepo>.git
git branch -M main
git push -u origin main
```

`.gitignore` כבר מסנן מפתחות, סיסמאות, נתונים אישיים ומטמונים — הם לא נכנסים ל‑git.

---

## שלב 2 — פריסה ב‑Render (חינם)

1. היכנס ל‑https://render.com והתחבר עם GitHub.
2. **New → Blueprint** → בחר את ה‑repo. Render יקרא את `render.yaml` שבשורש.
3. במסך שנפתח, מלא את המשתנה **`PNKS_AUTH_PASSWORD`** — סיסמת גישה חזקה.
   **בלי זה האתר לא יעלה.** כל מי שיגיע לכתובת יצטרך את הסיסמה הזו.
4. (אופציונלי) `AZURE_SPEECH_KEY` להקראה קולית אנושית יותר.
5. **Apply**. אחרי 3–5 דקות מקבלים כתובת כמו `https://hapinkas-hayomi.onrender.com`.

כל `git push` ל‑`main` מפרסם מחדש אוטומטית.

> **התוכנית החינמית "נרדמת"** אחרי 15 דקות בלי גישה — הכניסה הראשונה אחרי הפסקה לוקחת ~30 שניות.
> `plan: starter` (בתשלום) מבטל את זה.

---

## שלב 3 — בטלפון

פתח את הכתובת בדפדפן → הזן את הסיסמה → תפריט הדפדפן → **"הוסף למסך הבית"**.
נוצר אייקון אפליקציה. הסשן נשמר 30 יום.

---

## מה עובד בענן ומה לא

| עובד | דורש את המחשב בבית |
|---|---|
| מענה יומי, לוח עברי, פרשה, חגים | סיכום היום ב‑AI (Ollama מקומי) |
| תהילים, הגדה, מגילה | לימוד יומי מבואר (AI) |
| כספים — מדדים ותוכנית בסיסית | DevOps, n8n, Docker |
| שוק ההון, לוטו | עיצוב וידאו AIA (ffmpeg כבד) |
| דיבוב קולי (Google TTS) | מיילים ויומן (Maton), Outlook |
| חגים, ספירה, שבת | תיקיות עיצוב מקומיות |

מה שדורש את המחשב פשוט יציג "לא זמין" — הוא לא מפיל את השאר.

---

## הרצה מקומית — לא משתנה

ללא `PNKS_AUTH_PASSWORD`, המערכת רצה ב‑`localhost:4420` בדיוק כמו קודם, בלי מסך התחברות.
